# 04-robotics 设计说明

## 1. 为什么是运动学仿真器

四足控制器的职责是决定「脚该放在哪」。要检验这个决定，最干净的办法不是引入接触求
解器和一堆调参，而是**把接触约束直接解出来**：支撑相里足端在世界系中不动，于是机身
位姿就是「最能解释这些固定点」的那个刚体变换。

好处：闭式、确定性、可逆。坏处也很清楚——它不知道力，也不知道会不会翻。所以这个仓
库的定位是**步态逻辑的严格测试台**，不是机器人仿真器。所有结论都只关于运动学。

代价是必须自己面对一串只在「同时平移 + 旋转」时才暴露的坑，下面逐条记录。

---

## 2. 运动学

3-DOF 腿，每腿三个关节：γ（侧摆，绕机体 x 轴）、α（髋俯仰）、β（膝）。

```
膝相对髋：(0, 0, -l1)  绕 x 转 γ，再绕 y 转 α
踝相对膝：绕 y 转 β 之后，再沿小腿 -z 走 l2
```

`leg_ik` 闭式解：由足端相对髋的向量解 γ，投影到矢状面后用余弦定理解 β，再解 α。
可达半径 `|l1 - l2| < r < l1 + l2`，越界抛 `ValueError` 而不静默钳制，钳制交给
`clamp_reachable` 显式执行。

`leg_jacobian` 解析推导，与中心差分对拍误差 5.8e-10（相对量级 1e-9，来自差分本身）。

---

## 3. 步态轨迹：`foot_cycle`

支撑相足端相对机身匀速后滑 `stride = v · duty · T`，摆动相把它摆回去。关键是**两相
连接处速度要连续**，否则触地瞬间腿部会抖。

`_quintic` 是端点速度、加速度都匹配的五次 Hermite。摆动相用

```
v_hat = -speed · swing_time
dx = _quintic(-half, +half, v_hat, v_hat, tau)
```

其中 `swing_time = (1-duty)T`——Hermite 的速度项是 `d/dtau`，所以物理速度要乘摆动
时长才能落到同一量纲。支撑相斜率 `-stride/(duty·T)`，换算到 τ 之后正是 `v_hat`，
两端都接得上。

实测（`test_foot_cycle_matched_velocity`）：支撑相 -0.5000 m/s，摆动相起点 -0.5000 m/s。

---

## 4. 转向：`turn_cycle` 必须是零基的

一条腿落地后，机身继续转动，于是同一个世界坐标点在机体系里反向旋转。

`yaw_touchdown + θ(phase)` 必须对**所有正在支撑的腿**给出同一个 `yaw(t)`。把
`t - t_touchdown = φ·T` 和 `yaw(t - φT) = yaw(t) - wz·φT` 代进去，这条腿隐含的机身
偏航是

```
yaw(t) - wz·φT + θ(φ)
```

要它对每条腿都等于 `yaw(t)`，只能有 **θ(φ) = wz·φ·T**（支撑相内线性）。

任何附加常数都会破坏它——除非所有支撑腿共享同一相位。trot / pace / bound 恰好共享，
**crawl 不共享**：三条支撑腿处在三个不同相位，各自要求的偏航不同，最小二乘折中结果
是偏航滞后。实测损失 1.2°/s。

摆动相要把腿平滑送回，且两端角速度必须接上支撑相的 `wz`（`foot_cycle` 处理平移的
同一个坑）：

```
v = sweep·(1-duty)/duty          # 换算到 τ 单位
θ_swing = _quintic(sweep, 0, v, v, τ)
```

修好之后，四种步态 × 两档 duty 的偏航率误差 **0.0000 deg/s**。

---

## 5. 精确合成：`yaw_travel` 与 `foot_target`

这是最贵的一个坑，也是唯一会**发散**的一个。

支撑腿在世界系中不动，而机身在平移 + 旋转。它在机体系中的坐标不是
`R(-a)·r + dir·dx`（在当前机体系里加一个直线步长向量），而是

```
R(-a) · (r_td − G(t)·v),      G(t) = ∫₀ᵗ R(wz·s) ds,   a = wz·t
```

`G(t)` 是机身在**自己那个正在转动的坐标系**里走过的路径。`wz → 0` 时 `G = t·I`，
公式退化成朴素答案，所以这是包含而不是替代。

误差量级 `wz·|v|·t²/2`：`wz = 0.4`、`|v| = 0.3` 时一个支撑相（0.2 s）累计 2.4 mm。
单看不致命，但**每个支撑相都要重新锚定一次**，于是每步重新注入一次误差，累积成明显
的位姿尖峰并逐周期放大（实测 0.0023 → 0.0031 → 0.0041 → 0.0053 → 0.0065 → 0.0080 m）。

`foot_target` 把两件事合并成一个函数：

- 支撑相：上式，精确；
- 摆动相：从「抬脚点」到「下次落足点」的二维 Hermite，两端速度匹配支撑相速率
  （离地点 `-R(sweep)·v·swing_time`，落地点 `-v·swing_time`）。

`wz = 0` 时它逐位复现 `foot_cycle`（`test_foot_target_reduces_to_foot_cycle`，
一致到 5.6e-17）。保留一维版本的理由是它把步态**规格**写清楚了；两者用测试钉住。

修好之后 `vx=0.3 / wz=0.4` 的圆走：弧长速度 0.300000、偏航率 0.400000、高度偏差
1.1e-16 m。

---

## 6. 接触集调度：三个独立的坑

`KinematicSimulator` 每 tick 用「上一 tick 的接触集」解位姿，然后才更新接触集。顺序
和判据都踩过坑。

### 6.1 先更新接触集 → 每周期冻结两次

刚落地的脚，锚点按 `_to_world(feet_body)` 用**未更新的位姿**算出，于是解算问题以当前
位姿为精确解：机身整 tick 不动。trot 每周期两次。

代价：`2·dt·v = 0.0016 m` 每 0.16 m 步幅，**恰好 1% 的速度缺口**。修好之后 0.3960 →
0.40000 m/s。

### 6.2 用布尔 `phase < duty` 判着地 → 高度单向棘轮

`dz` 是连续量，相位布尔量会在真正离地**之前**翻转。浮点累积误差让某条腿在自己的最后
一个支撑 tick（`phase = 0.4999…`）才进接触集，下一 tick 已在摆动相（`dz = 8.6e-5`）
却仍是约束——解算把机身压低这个量，且**永不回升**。每次抬脚都是一次单向棘轮，下沉
速率约 0.5 mm/s。

注意这个 bug **只在浮点漂移跨过相位边界之后才出现**（实测 t ≈ 2.2 s，不是 0.4 s），
4 秒的测试看不见。修法：接触判据改用**离地高度** `dz ≤ PLANT_EPS`（1e-9）。两个有意义
的值差着天文数字——相位差 1e-16 时 `dz ~ 1e-32`，摆动一 tick 时 `dz ~ 9e-5`——任何
中间阈值都能干净分开。

### 6.3 交错落地 → 单点约束，偏航不可观测

crawl 相邻腿相位差 0.25。周期 0.5 s 配 dt 2 ms 时相位步进 0.004，**跨过**边界而不是
落在边界上（trot 的 0.4 s 周期整除，所以从不遇到）。于是在抬手那一 tick：旧腿已抬起
（不算着地）、新腿刚落地（还没有锚点），有效约束只剩 **1 个点，而单点无法观测偏航**。

`len(active) >= 2` 失败 → 整 tick 冻结，下一 tick 补回一半。每周期净损 0.0008284 rad，
即指令偏航率的 0.41%。

修法：约束不足时退回全部继承接触（抬脚腿此刻只离地 0.3 mm，且下一 tick 会被旧锚点把
高度拉回，不累积）。同时给 `turn_cycle` 补上角速度匹配——否则抬脚腿在 τ=0.004 处角
速度偏慢，仍留 0.0002 rad/tick 的不一致。

### 6.4 启动瞬态

`reset()` 看不到指令，只能把四足锚在**中性位姿**。任何偏航指令都在 t=0 就旋转了足端
（`turn_cycle(0)` 在零基版本里等于 0，但旧版本非零），于是锚点与首个规划不一致，第一
次解算取折中——实测一次性 -0.049 rad 偏航跳变，占 3 s 总转角的 3.4%。

修法：第一个 tick 拿到指令后按中性位姿重新锚定一次。

---

## 7. 位姿解算

`solve_body_pose` 用二维 Procrustes：支撑足在机体系中的 xy 与世界系锚点做最小二乘刚体
配准，z 取平均高差。两个点就够（4 个标量约束对 4 个未知量），三个以上是最小二乘。

`arctan2` 返回 (-π, π] 的折叠角。仿真器按上一采样解缠（unwrapped），否则机身偏航每半
圈跳 2π，任何由它算出的速率都是垃圾。

---

## 8. 稳定性与力分配

- `convex_hull`：单调链；`stability_margin`：点到凸多边形边界的有符号距离。
- `support_polygon`：支撑足凸包。**trot 的对角支撑是零面积多边形**（裕度 -inf）——这
  正是 trot 必须靠动态稳定、需要控制器存在的原因。
- **crawl 的三足三角形里，名义质心（机体原点）恰好落在对角线边上，裕度 0。** 切比雪夫
  中心（三角形内最深的点）有 6.7 cm 余量——所以静态爬行必须先横移机身，再抬腿。
- `zmp`：`x_zmp = x_com - (h/g)·a`，加速度 +1 m/s² 时后移 2.65 cm。
- `solve_forces`：6 维接触力旋量矩阵伪逆。四足对称载荷下每足 19.62 N（总重 8 kg），
  力矩残差 0。质心前移 10 cm 会把 52.6% 的载荷压到前足对上。
- `project_friction_cone`：对越锥的足**就地缩放切向分量**。注意这是**投影不是重解**：
  限幅后竖直旋量仍然满足，但切向总量已被破坏。50 N 前推（CoM 高 0.25 m，产生
  12.5 N·m 俯仰力矩，迫使载荷后移到后足对 72.1 N / 前足对 6.3 N）时，前足对剪切从
  12.5 N 被限到 1.9 N，**总共丢掉 21.2 N 剪切**。后足对因 fz 大而保有全部 12.5 N。
  要得到真正可行的分配需要 QP 或分层求解，这里只做诚实的检验与记录。

---

## 9. 测试清单（24 项）

```
[运动学]   FK/IK 往返 · 四腿名义位姿精确反解 · 雅可比对拍 · 越界拒绝
[步态]     stride 守恒与相位环绕连续 · 摆动速度匹配 · Raibert 符号
           转向切向性 + 零基线性 · 四步态×两档 duty 偏航率
           支撑足世界系静止 · foot_target 退化为 foot_cycle
[稳定性]   凸包/包含/裕度 · trot 零面积支撑 · crawl 三足裕度 · ZMP
[力分配]   旋量残差 · 摩擦锥投影
[仿真]     里程解算可逆 · 20 s 无高度棘轮 · 速度跟踪 · 反向/侧移
           原地转 · 平移+转向解耦
```

容差全部收紧到接近机器精度。**早期 1e-2 / 15° 的松容差正是让 1% 速度缺口和 3.4%
转角误差潜伏至今的原因**——记录在此以免重蹈。
